# Java / Maven
backend/target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Env / secrets
.env
.env.local
*.local

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Logs
*.log

# Rendered video intermediates. The finished MP4 is committed under
# frontend/public; the frames and padded WAVs that produced it are not — they
# are ~1900 PNGs and rebuild in a couple of minutes from build.mjs.
video/frames/
video/*.wav
video/voice.wav
video/audio-list.txt
video/node_modules
