# Base URL of the Continuum backend API.
# Leave empty for local dev (Vite proxies /api to localhost:8080).
# For production (e.g. Vercel), set to your deployed backend, e.g.:
#   VITE_API_BASE=https://continuum-api.onrender.com
VITE_API_BASE=
